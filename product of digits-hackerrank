#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,r,p;
    scanf("%d",&n);
    p=1;
    while(n!=0){
        
        r=n%10;
        if(r!=0){
        p=p*r;
        
        }
        n=n/10;
    }
    printf("%d",p);
    
    return 0;
}
