#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
char ch1[20],ch2[20];
    gets(ch1);
    gets(ch2);
  int i= strcmp(ch1,ch2);
    if(i==0)
        printf("Strings are same");
    else
        printf("Strings are not same");
    return 0;
}
