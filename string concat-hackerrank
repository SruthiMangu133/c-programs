#include <stdio.h>
#include <string.h>

int main() {
    
    char str1[100], str2[100];
    scanf("%s", str1);
    scanf("%s", str2);
    char result[201];
    strcpy(result, str1);    
    strcat(result, " ");
    strcat(result, str2);
    printf("%s\n", result);
    return 0;
}
