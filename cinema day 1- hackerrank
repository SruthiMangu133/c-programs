#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int d, m, y, std;
   scanf("%d", &d);
    scanf("%d", &m);
    scanf("%d", &y);
    scanf("%d", &std);
    int sum=d+m+y;
    int p = 3 * 4;
    if ((sum % p==0) && (std > 50) && (std < 100)) {
        printf("Cinema Day\n");
    } else {
        printf("Not a Cinema Day\n");
    }

    return 0;
}
 
