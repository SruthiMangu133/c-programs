#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int r,n,sum;
    scanf("%d",&n);
    sum=0;
    r=2;
    while(n>0){
        sum=sum+r;
        r=r*10+2;
        n--;
    }
    printf("%d",sum);
    return 0;
}
